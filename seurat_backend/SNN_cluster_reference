
工具构建要求：
参考qc.tool的建立格式，建立新的tools。先写r脚本，再根据r所需的参数，建立tools参数传入api.R的代码
在开始前，应该参考参数中的全部参数，在脚本开始处集中有条理地设置全部默认参数（默认使用小鼠物种），之后的部分再拆解送进来的json对必要的部分进行赋值。目前json格式没有确定，按照此分析需求进行确定，比如应该包含样本名字和地址的信息的字典（样本数量会变化）以及刚才所说的可选的可调整参数。
预设的的完整json格式以注释形式写在脚本中，避免使用多层嵌套形式，以变量名区分。
最终设定的参数除了少数关键值返回以外，大部分生成日志文件输出到固定地址data/新文件夹中。中间生成的图片也保存到data/新文件夹中。

从data文件夹中的PCA_UMAP中的固定json文件获取最后一个seurat_obj_filtered对象的名字再读取这个路径的对象。参考pca-umap.r中对于结果输出的代码。
获取的是pca结果的seurat_obj_filtered对象。

参数
{2. 图聚类分析 (Graph-based Clustering)
Seurat 基于 SNN (Shared Nearest Neighbor) 图算法进行聚类，分为建图和分群两步。

FindNeighbors (构建邻接图)
代码已用参数：

reduction & dims (1:15): 用于计算细胞间距离的维度。

verbose (FALSE): 关闭日志。

高优可调参数（未显式使用）：

k.param: 默认 20。构建 KNN 图时的邻居数量。如果你的目标细胞群非常小（比如某种稀有细胞），适当降低该值可以防止稀有细胞被大群“吞并”。

annoy.metric: 默认 "euclidean"（欧氏距离）。如果是极高维度的数据，有时可尝试 "cosine"（余弦相似度）。

FindClusters (社区发现分群)
代码已用参数：

resolution (0.8): 分群最核心参数。值越大，切分出的亚群越多。通常 3000 细胞左右的数据集在 0.4-1.2 之间。

高优可调参数（未显式使用）：

algorithm: 默认 1（Louvain 算法）。强烈建议在复杂数据集尝试 4（Leiden 算法）。Leiden 是 Louvain 的优化版，能避免产生断开连接的“假亚群”。

random.seed: 随机种子，保证聚类结果可重复。}


# 聚类分析（resolution=0.8，适合2000-3000个细胞）
seurat_obj_filtered <- FindNeighbors(
  object = seurat_obj_filtered,
  reduction = reduction_use,
  dims = 1:15,
  verbose = FALSE
) %>%
  FindClusters(
    resolution = 0.8 # 关键参数：分辨率越高，聚类数越多（推荐范围0.6-1.2）
  )
 
# 查看聚类结果（细胞数分布）
table(seurat_obj_filtered$seurat_clusters)
# 输出示例：
#  0   1   2   3   4   5   6   7   8 
# 320 289 256 231 210 198 187 176 151 
 
# 可视化UMAP聚类图（按聚类分群着色）
umap_clustered_plot <- DimPlot(
  seurat_obj_filtered,
  reduction = "umap",
  group.by = "seurat_clusters",
  pt.size = 0.3,
  label = TRUE, # 显示聚类编号
  label.size = 4
) + theme_paper()
print(umap_clustered_plot)
ggsave("plots/umap_clustered.pdf", umap_clustered_plot, width = 8, height = 6, dpi = 300)