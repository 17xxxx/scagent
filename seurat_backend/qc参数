一、 数据读取与对象创建1. Read10X()  用途：读取 10x Genomics Cell Ranger 输出的三个标准文件（barcodes, features, matrix），将其转换为稀疏矩阵。  核心参数：data.dir (已用)：包含文件的目录路径（或路径向量）。gene.column：指定 features.tsv 中哪一列作为基因名（默认 2，即 Gene Symbol）。cell.column：指定 barcodes.tsv 中哪一列作为细胞名（默认 1）。unique.features：是否强制基因名唯一（默认 TRUE，重复基因加 .1、.2）。strip.suffix：是否移除细胞 barcode 末尾的 -1 后缀（默认 FALSE）。2. CreateSeuratObject()  用途：将表达矩阵等原始数据封装为 Seurat 核心对象，并执行初步的底层过滤。  核心参数：counts (已用)：未标准化的原始计数矩阵。  project (已用)：项目或样本名称，会保存在 orig.ident 列。  min.cells (已用)：基因过滤阈值。保留至少在 N 个细胞中表达的基因。  min.features (已用)：细胞过滤阈值。保留至少检测到 N 个基因的细胞。  assay：指定数据的分析类型名称（默认 "RNA"）。meta.data：在创建对象时直接附加的用户自定义细胞级别的元数据数据框。二、 质量控制与过滤 (QC)3. PercentageFeatureSet()  用途：计算某一类特定基因（如线粒体、核糖体基因）在每个细胞总表达量中所占的百分比。  核心参数：object (已用)：Seurat 对象。  pattern (已用)：正则表达式模式，用于匹配基因名（如 ^MT-）。  features：直接传入一个基因名向量，计算这些基因的比例（与 pattern 二选一）。col.name：计算结果保存到 metadata 的列名（默认直接返回数值向量，通过 obj[["name"]] <- 赋值）。assay：指定使用哪个 assay 的数据。4. subset()  用途：根据元数据（如 QC 指标）或基因表达量过滤保留符合条件的细胞或基因。  核心参数：x (已用)：Seurat 对象。  subset (已用)：逻辑表达式，用于过滤细胞（如 nFeature_RNA > 200）。  features：用于保留特定的基因向量。idents：保留特定的细胞群（基于 Idents(object)）。downsample：随机下采样，限制每个细胞群最多保留的细胞数。seed：随机下采样的随机种子。5. merge()用途：将多个独立的 Seurat 对象合并为一个包含所有细胞的全局对象。核心参数：x (已用)：第一个 Seurat 对象。y (已用)：第二个 Seurat 对象，或包含多个对象的列表。add.cell.ids (已用)：字符向量，在细胞条形码前添加前缀，防止不同样本间的 barcode 重名。merge.data：是否同时合并经过标准化处理（data slot）的数据（默认 FALSE，仅合并原始 counts）。project：为合并后的新对象指定一个新的项目名称。三、 数据预处理6. NormalizeData()  用途：消除细胞间测序深度的差异，使数据具有可比性。  核心参数：object (已用)：Seurat 对象。  normalization.method (已用)：标准化方法。包括 "LogNormalize"（默认）、"CLR"（中心对数比变换）或 "RC"（相对计数）。  scale.factor (已用)：缩放因子（默认 10000）。  margin：如果使用 CLR，指定是按特征（1）还是按细胞（2）进行标准化。7. FindVariableFeatures()  用途：筛选在细胞间表达变异最大的基因（高变基因），用于后续降维。  核心参数：object (已用)：Seurat 对象。  selection.method (已用)：计算方法。包括 "vst"（默认，基于均值-方差关系）、"mean.var.plot" 或 "dispersion"。  nfeatures (已用)：要选择的高变基因数量（默认 2000）。  mean.cutoff / dispersion.cutoff：当不使用固定数量时，通过均值或离散度的阈值来筛选。clip.max：在 vst 方法中，限制标准化后表达量的最大值（默认 "auto"）。8. ScaleData()  用途：对表达数据进行线性转换（Z-score 化），使每个基因的平均表达量为 0，方差为 1，确保高表达基因不主导 PCA。  核心参数：object (已用)：Seurat 对象。  features (已用)：要缩放的基因向量（默认仅缩放高变基因，可改为所有基因）。  vars.to.regress (已用)：需要通过线性模型回归掉的干扰变量（如线粒体比例、细胞周期评分等）。  model.use：用于回归分析的模型，包括 "linear"（默认）、"poisson"、"negbinom"。do.scale / do.center：是否进行方差缩放和均值中心化（默认均为 TRUE）。scale.max：截断过大的缩放值，防止极值影响（默认 10）。四、 降维与批次整合9. RunPCA()  用途：主成分分析，将数千个高变基因的维度压缩到少数几个主成分（PCs）中。  核心参数：object (已用)：Seurat 对象。  features (已用)：用于计算 PCA 的基因（默认使用上一步鉴定的高变基因）。  npcs：计算的主成分数量（默认 50）。rev.pca：是否翻转 PCA 坐标轴。weight.by.var：是否根据主成分解释的方差对细胞的 PCA 坐标进行加权（默认 TRUE）。reduction.name：保存在对象中的降维结果名称（默认 "pca"）。10. RunHarmony()  用途：基于已有降维结果（如 PCA），通过迭代算法将不同批次的细胞在嵌入空间中拉近，消除批次效应。  核心参数：object (已用)：Seurat 对象。  group.by.vars (已用)：字符型或向量，指定 metadata 中代表批次的列名。  reduction.use (已用)：输入的降维结果名称（默认 "pca"）。  dims.use (已用)：用于校正的维度范围（如 1:30）。  reduction.save：保存校正结果的名称（默认 "harmony"）。theta：多样性惩罚参数。值越大，批次混合得越厉害（默认对于每个变量为 2）。可以为不同的 group.by.vars 传入向量。lambda：岭回归惩罚参数。值越小，细胞拉近的程度越大（默认 1）。sigma：软聚类（soft clustering）的核宽度（默认 0.1）。max.iter.harmony：Harmony 算法迭代的最大次数（默认 10）。max.iter.cluster：内部聚类迭代的最大次数（默认 20）。