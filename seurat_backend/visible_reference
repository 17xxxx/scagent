该参考文件是5个独立可视化tools的参考说明。

工具构建要求：
参考qc.tool的建立格式，建立新的tools。先写r脚本，再根据r所需的参数，建立tools参数传入api.R的代码
在开始前，应该参考参数中的全部参数，在脚本开始处集中有条理地设置全部默认参数（默认使用小鼠物种），之后的部分再拆解送进来的json对必要的部分进行赋值。目前json格式没有确定，按照此分析需求进行确定，比如应该包含样本名字和地址的信息的字典（样本数量会变化）以及刚才所说的可选的可调整参数。
预设的的完整json格式以注释形式写在脚本中，避免使用多层嵌套形式，以变量名区分。
最终设定的参数除了少数关键值返回以外，大部分生成日志文件输出到固定地址data/新文件夹中。中间生成的图片也保存到data/新文件夹中。
工具2（标记基因表达可视化（小提琴图 + 气泡图）和工具5（功能富集可视化（GO/KEGG））要求有必须传入的参数。工具2必须传入的参数是cluster_markers的列表，工具5必须传入的参数是特定组别的名字的列表。

读取的seurat对象均为：
从data文件夹中的cell type annotation中的固定json文件获取最后一个seurat_obj_filtered对象的名字再读取这个路径的对象。参考cell-type-annotation.r中对于结果输出的代码。
获取的是cell type annotation结果的seurat_obj_filtered对象。


参数
{
    1. 细胞分群可视化：DimPlot()
DimPlot 是 Seurat 中最常用的降维散点图绘制函数。

已用参数：

reduction ("umap"): 指定使用的降维坐标（如 umap, tsne, pca）。

group.by (c("orig.ident", "cell_type")): 指定按哪些元数据列进行着色。传入向量会同时生成多张图。

pt.size (0.3): 点的大小。

split.by ("orig.ident"): 按样本（或条件）将图拆分成多个子图，非常适合检查样本间的批次效应或细胞丰度差异。

未用但高价值的可调参数：

label (TRUE/FALSE): 设为 TRUE 会在每个聚类中心添加文本标签（如细胞类型名称），省去图例，让图面更直观。

repel (TRUE/FALSE): 配合 label = TRUE 使用，利用 ggrepel 包防止文本标签相互重叠。

cols: 自定义颜色向量。如果不喜欢默认的颜色，可以传入自定义调色板（如 cols = c("T_cell" = "red", "B_cell" = "blue")）。

order (TRUE/FALSE): 设为 TRUE 时，会将较小的细胞群绘制在顶层。在细胞严重重叠的区域，这可以防止稀有细胞被大群细胞遮挡。

raster (TRUE/FALSE): 如果细胞总数超过 10 万，强烈建议设为 TRUE，将散点图光栅化（转为像素图），极大减小 PDF 文件体积并加快渲染速度。

2. 标记基因表达可视化：VlnPlot() 与 DotPlot()
VlnPlot() (小提琴图)
已用参数：

features: 要绘制的基因名。

group.by ("cell_type"): x 轴的分组依据。

pt.size (0.1): 细胞散点的大小。设为 0 可以完全隐藏散点，只看提琴轮廓，使画面更干净。

ncol (2): 多基因作图时的列数。

未用但高价值的可调参数：

split.by: 非常强大！例如 split.by = "Condition"（对照 vs 治疗组），会在同一个细胞类型下画出两个并排的半边小提琴，直接对比不同组间的表达差异。

assay & layer (或旧版 slot): 默认使用 RNA assay 的 data 层（log 标准化数据）。有时你可能想看未标准化的 counts，可以调整此参数。

log (TRUE/FALSE): 设为 TRUE 会将 y 轴转为对数刻度，当某些基因表达量极高时，有助于看清低表达区域的分布。

DotPlot() (气泡图)
已用参数：

features: 基因列表。气泡图的 x 轴默认是基因。

group.by: y 轴分组依据。

dot.scale: 气泡的最大尺寸。

未用但高价值的可调参数：

cols: 气泡颜色的渐变范围。默认是 c("lightgrey", "blue")。常用组合有 c("lightgrey", "red") 或更丰富的渐变色谱。

col.min & col.max: 颜色映射的上下限。如果你有一个极高表达的“离群”基因拉垮了整体色带，可以通过调低 col.max（比如设为 2）来增强其他基因的色彩对比度。

cluster.idents (TRUE/FALSE): 设为 TRUE 会对 y 轴的细胞亚群进行层次聚类，并将聚类树画在图侧，表达模式相似的细胞群会自动靠拢。

3. 细胞类型比例可视化：geom_col() (基于 ggplot2)
这段代码是纯 R 语言数据框操作和 ggplot2 绘图。

已用参数：

position = "stack": 将柱子堆叠，y 轴代表绝对数量或计算好的百分比。

width: 柱子的宽度。

未用但高价值的可调参数（极力推荐修改）：

position = "fill": 这是绘制细胞比例图的最佳参数。 如果在 geom_col() 中使用它，无论原数据的绝对细胞数是多少，它都会自动将每根柱子的高度拉伸到 100%（比例 1.0），你甚至不需要在前面手动计算 ratio。

scale_fill_manual(values = ...): 用于统一配色。在多张不同类型的图中（如 UMAP 图、气泡图、柱状图），保持同一种细胞类型的颜色绝对一致是非常重要的。

4. 标记基因热图：DoHeatmap()
热图是单细胞文章中必不可少的“全局视野”图表。

已用参数：

features: 选定的 Top 基因集。

size, angle: 基因名标签的字体大小和旋转角度。

raster: 是否光栅化。

未用但高价值的可调参数：

disp.min & disp.max: 热图的灵魂参数。 默认是将 z-score 截断在 -2.5 到 2.5。如果热图看起来一片黑或者色彩对比度不强，可以调成 disp.min = -1.5, disp.max = 1.5，对比度会立刻爆表。

cells: 如果细胞总数太多（如 5 万个），直接画热图不仅慢，还会导致条带模糊。可以使用 cells = sample(Cells(seurat_obj), 5000) 随机抽取部分细胞作图，效果往往更好。

lines.width: 调整不同细胞群之间那条白色分隔线的粗细。

group.bar (TRUE/FALSE): 是否在热图顶部显示代表细胞群颜色的彩条，默认为 TRUE。

5. 功能富集可视化：enrichGO() 与 barplot()
基于 clusterProfiler 包的标准富集流程。

已用参数：

OrgDb: 物种数据库（如人类 org.Hs.eg.db）。

keyType ("SYMBOL"): 基因名格式。

ont ("BP"): 生物学过程（Biological Process）。还可以选 "CC" (细胞组分), "MF" (分子功能) 或 "ALL"。

qvalueCutoff, pAdjustMethod: 统计学检验过滤标准。

showCategory: 柱状图展示的通路数量。

未用但高价值的可调参数：

universe (enrichGO参数): 背景基因集。非常重要且常被忽略的参数。单细胞测序通常只能探测到 3000-8000 个基因，如果不设置 universe（默认使用全基因组约 2万个基因），会导致富集结果产生假阳性。建议将 universe 设置为该单细胞数据集中表达量大于 0 的所有基因。

readable (enrichGO参数): 如果 keyType 用的不是 SYMBOL 而是 ENTREZID，设为 TRUE 可以自动将富集结果里的基因 ID 转回易读的 SYMBOL 基因名。

}



八、核心可视化：从分群到功能验证

可视化的核心是 “清晰展示生物学结论”，以下是 scRNA-seq 分析必做的 6 类图表，覆盖分群、标记基因、细胞比例等核心信息。
1. 细胞分群可视化（UMAP/tSNE）

除了基础分群图，可按样本、批次、细胞类型分别可视化，展示数据分布：

    # 按样本+细胞类型联合可视化
    umap_sample_celltype <- DimPlot(
      seurat_obj_filtered,
      reduction = "umap",
      group.by = c("orig.ident", "cell_type"),
      pt.size = 0.3,
      split.by = "orig.ident" # 按样本拆分图
    ) + theme_paper()
    ggsave("plots/umap_sample_celltype.pdf", umap_sample_celltype, width = 12, height = 8, dpi = 300)

R
运行

2. 标记基因表达可视化（小提琴图 + 气泡图）

验证细胞类型注释的准确性，展示标记基因在不同细胞群的表达：

    # 选择关键标记基因（覆盖主要细胞类型）
    marker_genes <- c("CD3D", "MS4A1", "CD68", "EPCAM", "PECAM1", "COL1A1")
     
    # 1. 小提琴图（展示单个基因在各细胞群的表达分布）
    vln_marker_plot <- VlnPlot(
      seurat_obj_filtered,
      features = marker_genes,
      group.by = "cell_type",
      pt.size = 0.1,
      ncol = 2
    ) + theme_paper()
    ggsave("plots/marker_genes_vln.pdf", vln_marker_plot, width = 12, height = 10, dpi = 300)
     
    # 2. 气泡图（展示多个基因在各细胞群的表达水平和比例）
    dot_marker_plot <- DotPlot(
      seurat_obj_filtered,
      features = marker_genes,
      group.by = "cell_type",
      dot.scale = 8
    ) +
      theme_paper() +
      theme(axis.text.x = element_text(angle = 45, hjust = 1)) # x轴基因名旋转45度
    ggsave("plots/marker_genes_dot.pdf", dot_marker_plot, width = 10, height = 8, dpi = 300)

R
运行

3. 细胞类型比例可视化（堆叠柱状图）

比较不同样本 / 处理组的细胞类型比例差异：

    # 计算每个样本的细胞类型比例
    cell_type_ratio <- seurat_obj_filtered@meta.data %>%
      group_by(orig.ident, cell_type) %>%
      summarise(count = n(), .groups = "drop_last") %>%
      mutate(ratio = count / sum(count) * 100)
     
    # 绘制堆叠柱状图
    ratio_bar_plot <- ggplot(
      cell_type_ratio,
      aes(x = orig.ident, y = ratio, fill = cell_type)
    ) +
      geom_col(position = "stack", width = 0.7) +
      labs(x = "Sample", y = "Cell Type Ratio (%)", fill = "Cell Type") +
      theme_paper() +
      theme(axis.text.x = element_text(angle = 45, hjust = 1))
    ggsave("plots/cell_type_ratio.pdf", ratio_bar_plot, width = 10, height = 6, dpi = 300)

R
运行

4. 标记基因热图（Top 标记基因）

展示每个细胞群的 Top5 标记基因，直观反映聚类特异性：

    # 提取每个聚类的Top5标记基因
    top5_markers_per_cluster <- cluster_markers %>%
      group_by(cluster) %>%
      slice_head(n = 5) %>%
      pull(gene) %>%
      unique()
     
    # 绘制热图（按细胞类型分组，行=基因，列=细胞）
    heatmap_markers <- DoHeatmap(
      seurat_obj_filtered,
      features = top5_markers_per_cluster,
      group.by = "cell_type",
      size = 3, # 基因名字体大小
      angle = 0, # 基因名水平显示
      raster = FALSE # 非光栅化，放大不失真
    ) +
      scale_fill_viridis(option = "plasma") +
      theme_paper() +
      theme(legend.position = "right")
    ggsave("plots/markers_heatmap.pdf", heatmap_markers, width = 12, height = 10, dpi = 300)

R
运行

5. 功能富集可视化（GO/KEGG）

对关键细胞群（如差异细胞类型）做功能富集，揭示生物学功能：

    # 以巨噬细胞（cell_type="巨噬细胞"）为例，提取高表达基因
    macrophage_genes <- cluster_markers %>%
      filter(cluster == 1) %>% # 假设巨噬细胞是Cluster 1
      pull(gene)
     
    # GO生物学过程富集分析
    go_enrich <- enrichGO(
      gene = macrophage_genes,
      OrgDb = org.Hs.eg.db,
      keyType = "SYMBOL",
      ont = "BP", # 生物学过程
      pAdjustMethod = "fdr",
      qvalueCutoff = 0.05
    )
     
    # 可视化Top10富集通路（柱状图）
    go_bar_plot <- barplot(
      go_enrich,
      showCategory = 10,
      title = "Macrophage GO Biological Process Enrichment"
    ) + theme_paper()
    ggsave("plots/macrophage_go_enrich.pdf", go_bar_plot, width = 10, height = 6, dpi = 300)

R
运行
