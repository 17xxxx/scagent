Cell Type Annotation
工具构建要求：
参考qc.tool的建立格式，建立新的tools。先写r脚本，再根据r所需的参数，建立tools参数传入api.R的代码
在开始前，应该参考参数中的全部参数，在脚本开始处集中有条理地设置全部默认参数（默认使用小鼠物种），之后的部分再拆解送进来的json对必要的部分进行赋值。目前json格式没有确定，按照此分析需求进行确定，比如应该包含样本名字和地址的信息的字典（样本数量会变化）以及刚才所说的可选的可调整参数。
预设的的完整json格式以注释形式写在脚本中，避免使用多层嵌套形式，以变量名区分。
最终设定的参数除了少数关键值返回以外，大部分生成日志文件输出到固定地址data/新文件夹中。中间生成的图片也保存到data/新文件夹中。
r脚本最后在data下的新文件夹中创建一个叫做seuratobject的json文件，保存最后一次运行得到的rds文件名，之后的工具可以靠读取最后一个名字获取保存的变量。

所需seurat对象：
从data文件夹中的snn cluster中的固定json文件获取最后一个seurat_obj_filtered对象的名字再读取这个路径的对象。参考snn-cluster.r中对于结果输出的代码。
获取的是snn cluster结果的seurat_obj_filtered对象。

参数
{3. 标记基因识别 (Marker Gene Identification)
这步用于寻找每个聚类的高表达特征基因。

FindAllMarkers (寻找所有聚类的 Marker)
代码已用参数：

only.pos (TRUE): 仅返回上调的基因（通常做细胞鉴定只看高表达基因）。

min.pct (0.25): 基因至少在 25% 的细胞中表达，用于过滤低表达噪音，加快运算。

logfc.threshold (1): 差异表达的对数折叠变化阈值。注意 Seurat 默认是以自然对数 (ln) 计算的，如果想要严格的 2 倍差异，这里的值应根据底数调整。

test.use ("wilcox"): 使用 Wilcoxon 秩和检验。

高优可调参数（未显式使用）：

return.thresh: 默认 0.01。调整 P-value (或 padj) 的显著性阈值。

max.cells.per.ident: 默认无限制。当你的细胞数量极大（如 10万+）时，设置为 500 或 1000 可以对每个聚类进行下采样，在极大地提升计算速度的同时，几乎不影响 Marker 基因找寻结果。

assay: 默认活跃 Assay（通常是 RNA）。如果使用了 SCTransform，需明确指定寻找差异的 Assay。

4. 细胞类型注释 (Cell Type Annotation)
代码使用 SingleR 进行参考数据集比对注释。

GetAssayData (提取表达矩阵)
代码已用参数：

assay ("RNA"): 提取未校正的原始 Assay。

slot ("scale.data"): 提取标准化后的数据。（注：在 Seurat V5 中，slot 参数已被 layer 替代）。

高优可调参数（未显式使用）：

提取 counts (原始矩阵) 或 data (log标准化矩阵)。SingleR 官方通常推荐输入 log 标准化后的 data 而不是 scale.data。

SingleR (自动注释)
代码已用参数：

test, ref, labels: 测试集、参考集及参考集的标签。

clusters: 按 Seurat 聚类结果进行注释。这比逐个细胞注释（Single-cell 模式）速度快得多，且更能抵抗单细胞水平的 Dropout 噪音。

assay.type.test & assay.type.ref: 数据类型标识。

高优可调参数（未显式使用）：

de.method: 默认为 "classic"。用于在参考数据集中寻找 marker 基因的策略。可以更改为 "wilcox" 或 "t" 检验。

fine.tune: 默认 TRUE。SingleR 的精细调节功能，在高度相似的细胞群（如 CD4+ T 和 CD8+ T）难以区分时非常有用。}

# 筛选每个聚类的标记基因（log2FC>1，padj<0.05）
cluster_markers <- FindAllMarkers(
  object = seurat_obj_filtered,
  only.pos = TRUE, # 仅保留上调基因
  min.pct = 0.25, # 至少在25%的细胞中表达
  logfc.threshold = 1, # 最小log2折叠变化
  test.use = "wilcox" # 检验方法（单细胞常用Wilcoxon秩和检验）
)
 
# 查看每个聚类的Top5标记基因
top_markers <- cluster_markers %>%
  group_by(cluster) %>%
  slice_head(n = 5) %>%
  ungroup()
print(top_markers[, c("cluster", "gene", "avg_log2FC", "padj")])
 
# 保存所有标记基因到CSV
write.csv(cluster_markers, "results/cluster_markers.csv", row.names = FALSE)

# 加载人类参考数据集（HumanPrimaryCellAtlasData，含多种原代细胞）
ref_data <- HumanPrimaryCellAtlasData()
 
# 提取Seurat对象的表达矩阵（标准化后的数据）
expr_data <- GetAssayData(seurat_obj_filtered, assay = "RNA", slot = "scale.data")
 
自动注释
# 运行SingleR自动注释（按聚类注释，速度更快）
singleR_annot <- SingleR(
  test = expr_data,
  ref = ref_data,
  labels = ref_data$label.main, # 参考数据集的细胞类型标签
  clusters = seurat_obj_filtered$seurat_clusters, # 按聚类注释
  assay.type.test = "logcounts",
  assay.type.ref = "logcounts"
)
 
# 查看自动注释结果（每个聚类的预测细胞类型）
print(singleR_annot$labels)
 
# 将自动注释结果添加到Seurat对象
seurat_obj_filtered$cell_type_singleR <- unname(singleR_annot$labels[as.character(seurat_obj_filtered$seurat_clusters)])
 
annot_compare <- table(seurat_obj_filtered$cell_type_singleR)


 # 可视化注释后的UMAP图
umap_celltype_plot <- DimPlot(
  seurat_obj_filtered,
  reduction = "umap",
  group.by = "cell_type",
  pt.size = 0.3,
  label = TRUE,
  label.size = 3,
  repel = TRUE # 避免标签重叠
) + theme_paper()
print(umap_celltype_plot)
ggsave("plots/umap_celltype_annotated.pdf", umap_celltype_plot, width = 10, height = 8, dpi = 300)

