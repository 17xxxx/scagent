================================================================================
  scAgent 工具参数帮助文档
  工具总数: 10  |  生成时间: 2026-09-12 19:04
  本文件由 scripts/gen_tools_help.py 自动生成，请勿手工编辑。
================================================================================

  分析流程: QC → PCA/UMAP → SNN聚类 → 细胞注释
            └→ DimPlot / 标记基因可视化 / 细胞比例 / 热图 / 功能富集

  调用规则:
    · 工具 1-4 为流水线，必须按顺序执行，前一步成功后才能调用下一步
    · 工具 5-9 为独立可视化，均依赖工具 4 的输出
    · 不确定当前进度时，先调用 check_pipeline_status
    · 所有参数均可省略，省略时使用下表中的默认值

================================================================================
  1. run_qc_for_all_samples
================================================================================
  模块: tools/qc_tool.py

  用途:
    对 data/rawdata/ 目录下所有 10X 样本执行完整单细胞质量控制流程。

  参数:
  参数名                类型      默认值            说明
  ────────────────────────────────────────────────────────────────────────────
  project               str       scRNA_project     项目名称，决定输出文件前缀，
                                                    默认 scRNA_project
  min_cells             int       3                 基因至少出现在多少个细胞中，
                                                    默认 3
  min_features          int       200               细胞至少表达多少个基因，默
                                                    认 200
  nfeature_rna_low      int       200               nFeature_RNA 过滤下阈值，
                                                    默认 200
  nfeature_rna_high     int       6000              nFeature_RNA 过滤上阈值，
                                                    默认 6000
  percent_mt_max        int       5                 线粒体基因比例上限(%)，默
                                                    认 5
  percent_ribo_min      int       10                核糖体基因比例下限(%)，默
                                                    认 10
  run_harmony           bool      True              是否运行 Harmony 批次校正，
                                                    默认 true

================================================================================
  2. run_pca_umap
================================================================================
  模块: tools/pca_umap_tool.py

  用途:
    对 QC 完成的 Seurat 对象执行降维与可视化分析。

  参数:
  参数名                类型      默认值            说明
  ────────────────────────────────────────────────────────────────────────────
  project               str       scRNA_project     项目名称，决定输出文件前缀，
                                                    默认 scRNA_project
  elbow_ndims           int       50                ElbowPlot 展示的主成分数，
                                                    默认 50
  umap_dims             str       1:15              UMAP 输入的前 N 个主成分，
                                                    如 '1:15'，默认 1:15
  umap_n_neighbors      int       30                UMAP 邻近数，越大越保留全
                                                    局结构，默认 30
  umap_min_dist         float     0.3               UMAP 点间最小距离，越小细
                                                    胞越抱团，默认 0.3
  dimplot_group_by      str       orig.ident        DimPlot 分组列名，默认 ori
                                                    g.ident（按样本着色）

================================================================================
  3. run_snn_cluster
================================================================================
  模块: tools/snn_cluster_tool.py

  用途:
    对 PCA/UMAP 完成的 Seurat 对象执行 SNN 图聚类分析。

  参数:
  参数名                类型      默认值            说明
  ────────────────────────────────────────────────────────────────────────────
  project               str       scRNA_project     项目名称，决定输出文件前缀，
                                                    默认 scRNA_project
  findneighbor_dims     str       1:15              FindNeighbors 输入维度范围，
                                                    如 '1:15'，默认 1:15
  k_param               int       20                KNN 邻居数，稀有细胞群可适
                                                    当降低，默认 20
  resolution            float     0.8               分群分辨率，越大亚群越多，
                                                    3000 细胞推荐 0.4-1.2，默
                                                    认 0.8
  cluster_algorithm     int       1                 聚类算法：1=Louvain，4=Lei
                                                    den（推荐复杂数据集），默
                                                    认 1

================================================================================
  4. run_cell_type_annotation
================================================================================
  模块: tools/cell_annotation_tool.py

  用途:
    对 SNN 聚类完成的 Seurat 对象执行标记基因识别与细胞类型自动注释。

  参数:
  参数名                类型      默认值            说明
  ────────────────────────────────────────────────────────────────────────────
  project               str       scRNA_project     项目名称，决定输出文件前缀，
                                                    默认 scRNA_project
  species               str       mouse             物种：mouse（默认，MouseRN
                                                    AseqData）或 human（HumanP
                                                    rimaryCellAtlasData）
  logfc_threshold       float     0.25              差异表达 logFC 阈值，默认 
                                                    0.25（Seurat 官方值，不宜
                                                    设太高）
  max_cells_per_ident   int       None              每个聚类最多采样细胞数，设
                                                     500-1000 可加速，默认不限
  singler_de_method     str       classic           SingleR 差异分析方法：clas
                                                    sic / wilcox / t，默认 cla
                                                    ssic

================================================================================
  5. run_dimplot
================================================================================
  模块: tools/dimplot_tool.py

  用途:
    对已完成细胞注释的数据生成 UMAP DimPlot 分群可视化。

  参数:
  参数名                类型      默认值            说明
  ────────────────────────────────────────────────────────────────────────────
  project               str       scRNA_project     项目名称，默认 scRNA_proje
                                                    ct
  reduction             str       umap              降维类型: umap, pca，默认 
                                                    umap
  group_by              list[str] ['orig.ident', 'cell_type']分组着色列，默认 [orig.ide
                                                    nt, cell_type]
  split_by              str       None              按该列拆分子图（如 orig.id
                                                    ent）。默认 None = 不拆分，
                                                    与 R 端 05_dimplot.R 的默
                                                    认值保持一致
  pt_size               float     0.3               点大小
  label                 bool      True              显示聚类/细胞类型标签
  repel                 bool      True              标签防重叠

================================================================================
  6. run_marker_visualization
================================================================================
  模块: tools/marker_viz_tool.py

  用途:
    对指定标记基因生成小提琴图和气泡图。

  参数:
  参数名                类型      默认值            说明
  ────────────────────────────────────────────────────────────────────────────
  marker_genes          list[str] Field(..., description="【必须】需要可视化的基因名列表，如 ['CD3D','MS4A1','CD68']")【必须】需要可视化的基因名
                                                    列表，如 ['CD3D','MS4A1','
                                                    CD68']
  project               str       scRNA_project     项目名称
  group_by              str       cell_type         分组列名
  pt_size               float     0.1               小提琴图散点大小，0=隐藏散
                                                    点
  ncol                  int       2                 多基因作图列数

================================================================================
  7. run_cell_ratio_viz
================================================================================
  模块: tools/cell_ratio_tool.py

  用途:
    按样本统计细胞类型比例，绘制堆叠柱状图。

  参数:
  参数名                类型      默认值            说明
  ────────────────────────────────────────────────────────────────────────────
  project               str       scRNA_project     项目名称
  group_by_sample       str       orig.ident        样本分组的列名，默认 orig.
                                                    ident
  group_by_celltype     str       cell_type         细胞类型列名，默认 cell_ty
                                                    pe
  position              str       stack             柱状图类型: stack(堆叠) 或
                                                     fill(100%填充)

================================================================================
  8. run_heatmap
================================================================================
  模块: tools/heatmap_tool.py

  用途:
    对各细胞群的 Top N 标记基因绘制热图。

  参数:
  参数名                类型      默认值            说明
  ────────────────────────────────────────────────────────────────────────────
  project               str       scRNA_project     项目名称
  top_n                 int       5                 每个聚类的 Top N 标记基因
  group_by              str       cell_type         分组列名
  sample_n              int       None              随机抽样细胞数（NULL=全部），
                                                    大规模数据建议设5000

================================================================================
  9. run_enrichment_analysis
================================================================================
  模块: tools/enrichment_tool.py

  用途:
    对指定细胞群做 GO 生物学功能富集分析。

  参数:
  参数名                类型      默认值            说明
  ────────────────────────────────────────────────────────────────────────────
  cell_group_names      list[str] Field(..., description="【必须】目标细胞类型名称列表，如 ['T_cells', 'B_cells']")【必须】目标细胞类型名称列
                                                    表，如 ['T_cells', 'B_cell
                                                    s']
  project               str       scRNA_project     项目名称
  species               str       mouse             物种: mouse 或 human
  ont                   str       BP                GO 本体: BP(生物过程)/CC(
                                                    细胞组分)/MF(分子功能)/ALL
  qvalue_cutoff         float     0.05              q 值过滤阈值
  show_category         int       10                展示的 Top N 通路数

================================================================================
  10. check_pipeline_status
================================================================================
  模块: tools/check_status_tool.py

  用途:
    查询单细胞分析流水线的当前进度和下一步建议。

  参数:
  参数名                类型      默认值            说明
  ────────────────────────────────────────────────────────────────────────────
  project               str       scRNA_project     项目名称
  data_dir              str       DATA_DIR          数据根目录

================================================================================
  文档结束
================================================================================
